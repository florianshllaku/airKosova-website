# Copy this file to `.env`

# Server
PORT=3000
NODE_ENV=development

# Target flight site to automate
TARGET_SITE_HOME=https://sys.prishtinaticket.net/

# Browser visibility (set false for headless)
OPEN_BROWSER_ON_SEARCH=false

# Keep the browser open after scraping (ms). Default 0 = close ASAP.
KEEP_BROWSER_OPEN_MS=0

# ========================================
# Speed / Headless Options (Playwright)
# ========================================
# Force headless mode regardless of Windows defaults / UI checkbox.
SCRAPER_FORCE_HEADLESS=true

# Enable a few safe speed tweaks (tighter timeouts + shorter post-click delay).
# Default: true when headless, false when headed.
SCRAPER_FAST_MODE=true

# Block heavy resource types to speed up page load.
# Default: true when SCRAPER_FAST_MODE=true
SCRAPER_BLOCK_RESOURCES=true

# Keep tabs ready between searches (recommended).
SCRAPER_KEEP_READY=true

# ========================================
# Concurrency pool (multi-request handling)
# ========================================
# Run multiple searches at the same time by keeping multiple browsers and tabs ready.
# Total concurrency = SCRAPER_POOL_BROWSERS * SCRAPER_POOL_TABS_PER_BROWSER
SCRAPER_POOL_BROWSERS=2
SCRAPER_POOL_TABS_PER_BROWSER=8

# Warm up all tabs by pre-loading the provider homepage on startup.
SCRAPER_POOL_WARMUP=true

# ========================================
# Supabase Authentication
# ========================================
# Get these from your Supabase project: Settings -> API
# Project URL
SUPABASE_URL=https://your-project.supabase.co
# anon public key
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

