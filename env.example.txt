# ========================================
# AirKosova Production Configuration
# ========================================
# Rename this file to .env and configure for your environment

# Node Environment (development | production)
NODE_ENV=production

# Server Port
PORT=3000

# Session Secret (generate a random string for production)
SESSION_SECRET=your-super-secret-session-key-change-this

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# ========================================
# Scraper Configuration
# ========================================

# Show browser window during scraping (true | false)
# Set to 'true' for debugging, 'false' for production
DEBUG_BROWSER=false

# Enable verbose logging (true | false)
VERBOSE_LOG=false

# Custom Puppeteer executable path (optional)
# Uncomment and set if Puppeteer can't find Chrome
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser

